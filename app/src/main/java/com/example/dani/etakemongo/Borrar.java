package com.example.dani.etakemongo;

import android.os.Bundle;

/**
 * Created by Dani on 25/05/2017.
 */

public class Borrar extends AjustesUsuario {
//tiene que extender de Modificar Usuario, incluir funcion borrar en positiveButton

   @Override
    public void onCreateDialog(Bundle savedInstanceState) {

/*
        AlertDialog.Builder builder = new AlertDialog.Builder(getActivity());

        builder.setMessage("¿Seguro que quiere eliminar su cuenta?")
                .setTitle("Confirmacion")
                .setPositiveButton("Si", new DialogInterface.OnClickListener()  {
                    public void onClick(DialogInterface dialog, int id) {
                        Log.i("Dialogos", "Confirmacion Aceptada.");
                        dialog.cancel();
                    }
                })
                .setNegativeButton("No", new DialogInterface.OnClickListener() {
                    public void onClick(DialogInterface dialog, int id) {
                        Log.i("Dialogos", "Confirmacion Cancelada.");
                        dialog.cancel();
                    }
                });

        return builder.create();
            }
}


*/
       }
}